{
  "$comment": "Criteria taken from issue #16 Acceptance Criteria (in-repo version bump). Not inferred from the title.",
  "authority_id": "16",
  "criteria": [
    {
      "check": "command_exit",
      "target": "grep -q '^VERSION ?= 1.0.9$' Makefile"
    },
    {
      "check": "command_exit",
      "target": "grep -q '^version: 1.0.9$' helm/jupyter-notebook-validator-operator/Chart.yaml"
    },
    {
      "check": "command_exit",
      "target": "grep -q 'jupyter-notebook-validator-operator.v1.0.9' bundle/manifests/jupyter-notebook-validator-operator.clusterserviceversion.yaml"
    },
    {
      "check": "command_exit",
      "target": "grep -q '\\[1.0.9\\]' CHANGELOG.md"
    }
  ],
  "covers": {
    "declared": "Makefile VERSION, Helm chart version, bundle CSV name, and CHANGELOG section for 1.0.9",
    "uncovered": "Quay image/bundle push; OperatorHub PRs; live OCP 4.20 install; issue text still asks for spec.replaces which ADR-055 replaced with olm.skipRange \u2014 that field is not this bar"
  }
}
